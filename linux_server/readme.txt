1.使用makefile的make编译出可执行程序
2.运行方法：./my_server 55555 10(55555是端口，10是线程池的线程数量)
3.发起连接:nc -v ip 端口（比如:nc -v 127.0.0.1 55555）
4.与服务端的交互内容都需要通过base64命令来编码解码，比如你需要发给服务器内容，需要使用base64命令编码后发送；
  服务的给你的内容也需要base64解码后查看。base64在linux有自带的。
